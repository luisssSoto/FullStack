// Implementar la función cantidadDeElementos(), donde deberá imprimir en consola la 
// cantidad de elementos que contiene el array ya declarado, utilizando uno de los 
// métodos vistos.

let array = [6, 8, 4, 9, 1, 6, 7];

function cantidadDeElementos() {
    console.log(array.length);
}

cantidadDeElementos(array);