// Implementar la función obtenerUltimoNumero(), donde deberá imprimir en consola el 
// ultimo registro del array ya declarado, utilizando uno de los métodos vistos.

let array = [6, 8, 4, 9, 1];

function obtenerUltimoNumero() {
    console.log(array.pop());   
}
obtenerUltimoNumero(array);