// Implementar la función testLoop, que mediante la utilización de un Loop For Of, 
// recorra mi Array e imprima en consola cada uno de los elementos que contiene 
// el array.

let miArray = ['Mensaje 1', 'Mensaje 2', 'Mensaje 3'];

function testLoop() {
    for(let elemento of miArray){
        console.log(elemento);
    }
}

