/**
 * Many array methods take a callback function as an argument. 
 * The callback function is called sequentially and at most once 
 * for each element in the array, and the return value of the 
 * callback function is used to determine the return value of 
 * the method. They all share the same signature:
 */